[project]
name = "sdlc-user-story-refiner"
version = "0.1.0"
description = "User Story Refiner Agent for refining and improving user stories in software development."
authors = [
    { name = "Agata Golebiowska", email = "gagata@google.com" },
    { name = "Tomasz Switon", email = "switon@google.com" },
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-aiplatform[adk,agent-engines]>=1.93.0",
    "google-genai>=1.9.0",
    "pydantic>=2.10.6",
    "pydantic-settings>=2.7.1",
    "python-dotenv>=1.0.1",
    "google-adk>=1.0.0",
    "sqlalchemy>=2.0.38",
    "google-cloud-spanner>=3.49.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.2",
    "pytest-asyncio>=0.23.7",
    "google-adk[eval]>=1.0.0",
    "nest-asyncio>=1.6.0",
    "agent-starter-pack>=0.14.1",
]

deployment = [
    "absl-py>=2.2.1",
]

[project.optional-dependencies]
lint = [
    "ruff>=0.4.6",
    "mypy>=1.15.0",
    "codespell>=2.2.0",
    "types-pyyaml>=6.0.12.20240917",
    "types-requests>=2.32.0.20240914",
]

[tool.ruff]
# extends the ruff rules established in the root .toml
extend = "../../../pyproject.toml"

[tool.ruff.lint]
# Ignores function complication rule throughout sample
ignore = ["C901", "PLR0915"]

[tool.ruff.lint.per-file-ignores]
# ignores unused import rule exclusively for this sample’s __init__.py files
"__init__.py" = ["F401"]

[tool.ruff.lint.isort]
known-first-party = ["app", "bidi_demo"]

[tool.hatch.build.targets.wheel]
packages = ["sdlc_user_story_refiner"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.agent-starter-pack]
example_question = "I want to build a Slack integration for our notification system. When a critical system alert triggers in our app, it should automatically post a formatted message to a specific Slack channel. We'll need to handle OAuth for Slack workspaces."

[tool.agent-starter-pack.settings]
agent_directory = "sdlc_user_story_refiner"

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
